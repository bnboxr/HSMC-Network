{
  "name": "hsmc-bridge-connectors",
  "version": "1.0.0",
  "private": true,
  "description": "Multi-chain bridge connectors for HSMC Network — BTC, ETH, BSC",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@solana/web3.js": "^1.98.4",
    "ethers": "^6.13.0"
  },
  "devDependencies": {
    "@types/node": "^20.19.43",
    "typescript": "^5.9.3"
  }
}
